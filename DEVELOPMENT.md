# 日迹：第一版开发约定

## 产品方向与当前实现

2026-09-15，产品目标调整为「个人电量显示器」：睡眠、进食、弟弟的安慰等是个人充电来源，工作、人际支出与琐事可以形成放电记录，展示当前电量和充放电收支。最新产品讨论以 [PRD.md](./PRD.md) 为准，早期讨论见 [历史 PRD](./docs/PRD-ARCHIVE.md)。

当前优先试用微信原生小程序。`miniprogram/` 已实现感受起点、主动记录、中性饮品、进食详情、开放计时、三页花园视觉和每日分享图；本地与微信模拟器验收结果见 [交付记录](./docs/RELEASE-20260916.md)，真机权限仍需确认。`src/` 保留原网页日历原型，不会自动同步或转换旧记录。

## 小程序模块

- `miniprogram/lib/energy.js`：纯计算、校准、事件修改、计时与备份格式校验。
- `miniprogram/lib/storage.js`：微信本机存储、损坏数据保护、覆盖恢复前备份。
- `miniprogram/lib/garden.js`：按真实记录选择有序的窗景花园阶段与弟弟，三个页面共用。
- `miniprogram/lib/layout.js`：手机窗口、键盘避让与弹层稳定尺寸。
- `miniprogram/lib/poster.js`：每日 PNG，名称默认展示；长名称完整换行、按实际高度分页，支持竖版与方版；备注始终不进入图片。
- `miniprogram/pages/index/`：电量、日历、回响三视图和记录表单。
- `scripts/tests/energy.test.cjs`：计算与存储边界测试。
- `scripts/check-miniprogram.mjs`：结构与本机微信编译器检查。
- `scripts/preview-miniprogram.mjs`：真实编译模板与页面逻辑的浏览器辅助验证，不替代微信环境。
- `scripts/smoke-wechat.mjs`：连接微信自动化端口，真实页面与画布验收；测试数据仅留在内存，不覆盖用户本机存储。

## 原有网页模块

- `src/model.ts`：日程、日期、分类、主观状态、统计及隔离的示例数据。
- `src/storage.ts`：本地持久化、备份验证及合并恢复。
- `src/calendar.ts`：使用 ICAL.js 导入 ICS，处理重复日程、例外、时区定义和重复导入。导入展开范围为当前月前后各六个月。不能识别的时区跳过并提示，不猜测时区。
- `src/share.ts`：Canvas 生成日/月 PNG，默认隐藏日程标题、感受原文与精力。
- `src/main.ts` / `src/styles.css`：响应式日历、每日记录、分享和数据设置。
- PWA：生产构建生成安装清单与离线缓存。

## 开发与部署

要求 Node.js 22.12+ 或兼容的新版本。安装依赖：`npm install`。开发：`npm run dev`。验证：`npm test`。生产构建：`npm run build`。本地预览生产版本：`npm run preview`。

部署目录为 `dist`，使用站点根路径，可交给支持静态站点的 HTTPS 托管平台。无需自购域名；实际平台、地区访问表现与套餐上线前确认。本阶段没有后端或云账号，不产生应用后端的运行费用。依赖版本及锁文件保存在项目中。

## 原有网页版边界

- 真实数据只在当前浏览器中。两个人、两台设备分别使用，不自动同步。清除站点数据会丢失记录，设置提供备份与合并恢复。
- 示例模式不写入真实记录。示例分享图带有示例标记。
- ICS 是用户主动导入的文件快照，不是 Google / Apple / Windows 日历实时授权。日历时间代表安排，不推断已完成；重叠时段在总时长中去重，分类时长分别计算。全天事件不折算成 24 小时。
- 精力来自主观选择，不根据咖啡或睡眠制造精确电量分数。
- 饮食提示仅在页面内显示；关闭页面后没有可靠定时通知。推送订阅、定时后端与提醒偏好待下一阶段实现。
- iPhone 使用 HTTPS 站点并添加到主屏幕。Apple Watch 健康读取和手表独立应用不在网页首版范围内。未来可添加原生 iPhone/Watch 端；Web Push 可按系统设置出现在配对手表，但不是手表应用。
- 感受是本地日记，尚未接入 AI 聊天。

## 后续顺序

1. 导入本地小程序，在微信模拟器编译，再用真实 iPhone 验证记录、后台计时、日历、相册和备份。
2. 两人试用并调整电量模型、快捷项和记录负担，5 / 10 / 20 点只作为试用参数。
3. 根据反馈增加可保存的个人快捷项、月度分享与更丰富的回看；现阶段只有自定义事件和每日图片。
4. 如需分发体验版，在微信后台配置体验成员；不把本地验证等同于已上传或已发布。
5. 根据试用结果再选择实时日历、睡眠来源、跨设备同步、提醒和 Watch 方案。

平台能力参考：https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/ ，https://webkit.org/blog/13966/webkit-features-in-safari-16-4/
