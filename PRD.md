# 日迹：生活的花园与个人电量

文档版本 v0.8 · 2026-09-19 · 对应小程序 0.6.0

## 产品目的

让人看见自己的生活，而不是督促人把电量、专注或记录凑满。

电量表达当下的主观余量；花园表达经历留下的形状。补充、消耗和只是记录，都可以进入花园。漂亮、可收藏和可分享的回响，是继续记录的重要动机。

最早的网页日历原型仍留在 `src/`，与当前 `miniprogram/` 分开运行、分开存储。本版不接入真实日历、Apple Watch、健康数据或云同步。

历史方案完整保存在 [v0.5 讨论稿](docs/PRD-ENERGY-V05.md) 和 [早期 PRD](docs/PRD-ARCHIVE.md)。以下规则取代其中默认 100、首页收支表、数值选择器和旧视觉配色。

## 首版范围

- 首次询问「现在感受怎么样？」：很累、有点累、还可以、有精神。不要求选择数字，也可以暂时关闭；未选择时不伪造电量。
- 选择经历后点击「记下此刻」才保存。一次确认只留一条记录。名称、时长、发生时间、影响程度和私人备注可在详情里补充。
- 不做饭点催促、漏餐推断、红点、通知、订阅消息或后台提醒。设备时间用于发生时间、归档、补记、主动计时及有界的自然消耗估算，不要求后台持续运行。
- 记录时提供短暂的数字与花园变化，撤销和编辑按真实记录重新计算。
- 所有业务数据保存在当前设备，没有开发者业务服务器。清理小程序或换设备前需要用户备份。

## 记录与感受

| 内容 | 默认记录方式 | 可选详情 |
| --- | --- | --- |
| 弟弟靠过来、眯了一会儿、终于到家 | 补充 | 一句话、实际发生/醒来时间、分钟数 |
| 通勤挤累了、一段工作、人际支出、莫名其妙掉电、晒得一身汗、淋雨湿了鞋 | 消耗 | 持续时间、影响程度、自己的名称 |
| 吃上饭了 | 补充 | 吃了/还没吃/这顿没吃、发生时间、饱腹、满足感 |
| 咖啡、奶茶、补剂 | 只是记一下 | 自定义名称、份量文字、实际时间；可主动改为补充或消耗 |
| 自己写一句 | 沿用当前补充/消耗选择 | 可改为只是记一下，私人备注可空 |
| 开放式计时 | 用户主动结束后确认 | 可改名称、方向和影响，或结束不留记录 |

吃饭的状态、饱腹与满足分开保存。「没吃」不自动扣电；后来吃饭可以新记一个片刻，或编辑原状态，不把未吃转换成第二笔消耗。不为咖啡、奶茶或补剂编造恢复效果、剂量建议或睡眠预测。

不展示单次 `+5/+10/-20`，不让用户调数值。可选影响为「一点 / 明显 / 很多」，也可以不选。时长不是必填项。

## 内部估算规则

这些参数仅供试用，不是医学或生理测量，也不用于不同用户之间比较。

1. 四种感受暂对应 20 / 40 / 60 / 80 的内部起点。首次无选择时显示待建立状态，不默认满电。
2. 可选影响对应 5 / 10 / 20；没有选择时，已知持续时长不超过 15 分钟为 5，不超过 120 分钟为 10，更长为 20。不会随时长无限累加。
3. 未填时长和程度的常规补充/消耗为 10。用餐详情中「没吃饱」为 5；否则「吃美了」为 20；其余为 10。用户手选影响优先于这些默认规则。饱腹事实不等于一定有益或有害，方向仍可以自己选择。
4. 中性记录为 0。未吃状态固定中性。它们仍能为花园增加形态。
5. 以最近一次感受为起点，按事件发生时间和同毫秒序号计算；早于起点的补记只归档，不重复增加或扣减电量。
6. 已知时长跨过起点时，只按起点后占完整区段的比例计入事件影响，保留内部小数、最后显示取整。未知时长不臆造分段。
7. 自然消耗暂设每小时 3 点。从最近起点算至当天午夜或 12 小时后，取较早者。已记录且有时长的睡眠区段合并去重后排除。无法知道未记录的睡眠；用户可补记或用现在感受修正。
8. 自然消耗最低至 0，不单凭时间制造透支。显式消耗记录仍可形成负值。跨天或超过本轮 12 小时后暂停自然消耗，显示「上次估计」，不追扣多天、不自动满电、不弹窗催促；新的感受开启新估算段。
9. 补充按剩余空间收敛：实际增量 = 名义影响 × (100 - 当前非负余量) / 100。事件只能补至 99，不能通过刷记录制造 100；已有用户校准的 100 不被补充反向扣减。透支时不衰减补充。内部不逐笔取整，避免小额重复记录利用舍入凑分。
10. 带时长且没有手选程度的消耗，其名义估计包含普通时间消耗，扣除同一区段已覆盖的自然消耗额度，只计其余部分。手选影响表示额外负担，不再抵扣；未知时长的事件按额外影响处理。
11. 删除、编辑、撤销重算派生结果；校准本身不是充放电事件，不抹掉历史。起点之前的记录不重新影响当前感受。

v1 / v2 升级先按旧模型计算现值，保存原始副本，然后将该现值和升级时刻设为 v3 过渡起点。不会追溯扣除升级前的时长，不改变旧名称、时间、顺序和名义影响。过渡起点会持久化，不在每次打开时重新建立。用户随时可以用现在的感受替换它；更早的历史修改只改变归档，不推翻这个过渡起点。

内部保留日事件名义充放电合计与补回比，分母为零返回空值；它们不包含自然消耗或补充衰减，不等同于新模型净变化。本版不在首页或分享图展示这些合计。

## 三个页面

### 电量

小标题、当前估算、一句不评价人的文案、今天的花园。下面依次是补充/消耗、常用经历、详情/自己写一句/饮品补剂、确认记录、最近片刻与撤销。开始计时在设置内，运行中的计时才出现在首页。

低电量不改变按钮位置、不把工作隐藏、不出现告警或枯萎。未设起点、过期估计、满电和透支有各自状态。失败不播放保存成功动效。

### 日历

只保留月份切换、周一开始的月历、每天一个小标记、选中日期的一行摘要和安静的小花园。没有记录的日子不填示例；未来日期不预造经历。支持四、五、六周月份，切月同步选中日期。

点摘要展开当天记录，才展示名称、时间、方向、私人备注及修改入口。月历主屏不堆叠收支、清单和重复的「查看一天」。

### 回响

以「这一天，都算数。」为主标题，日期、当天花园和记录名称构成图片。默认展示名称，备注始终不进入图片。隐藏名称不代表无法从花园识别某种经历，分享前以预览为准。

支持竖版和真正 1:1 方版；长名称完整换行，按实际占用空间分页，不静默截断。保存按钮只保存当前页。空白日不生成虚构事件或弟弟陪伴。

## 视觉与动效

以 [V10 三页方案](docs/concepts/didi-three-pages-motion-v10.md) 为参照：纸白、黑墨、青瓷绿、朱红、少量稻草黄和法式蓝，早期时尚封面的手绘植物感。标题克制，日历安静，避免幼态卡通和绩效仪表盘。

弟弟按真实凯米尔色美系缅因绘制：浅奶油银金长毛、耳毛、粉鼻、蓬松胸毛与大爪子。仅相关日期实际记录弟弟陪伴时出现，不把他自动添加到所有人的每天。原始私人照片不进入代码仓库。

- 保存成功：数字在 420ms 内过渡，补充轻抬、消耗轻落并短暂变色后回到墨色；无实际变化则不播放虚假的数字升降。
- 新经历：旧画面保留到新图加载完成，再以约 1800ms 自下而上的柔边揭示过渡；中性和消耗同样触发。不直接先换图再闪动。
- 弟弟：有真实陪伴记录时随新场景渐入，大爪子完整保留；目前不是独立爪子、眨眼或逐根植物的骨骼动画。
- 返回页面、翻页、重新打开不重播成长，不制造后台循环。设置可减少动效。
- 花园使用四个预先构图的阶段：0 条小苗、1 条初长、2–3 条舒展、4 条及以上丰盛，分别有无弟弟两个版本。所有经历保留；同阶段继续记录有一次轻柔舒展反馈，不随机叠图或无上限塞花。阶段动画不等于每条记录独立多一朵花。
- 四套花束按日期轮换：珊瑚牡丹、蓝鸢尾与银莲花、郁金香与虞美人、原红色马蹄莲。从 2026-09-19 开始循环，更早日期保留原马蹄莲；同一天的模板不受刷新、事件方向、数量、弟弟是否出现影响。所有页面按同一日期映射，未来扩展应增加日期分段而非改变旧映射。无需用户挑选模板，不新增设置负担。
- 手动输入名称在第一行直接显示，私人备注独立存储。输入层使用固定行高、稳定窗口基准和显式滚动高度，键盘收起后恢复，不因切页累积缩放。

## 隐私与验收

接口用途见 [隐私审核说明](docs/WECHAT-PRIVACY-REVIEW.md)。保留相册写入和剪贴板写入，没有新增相册读取、剪贴板读取、健康读取或订阅提醒权限。

实现、导入路径、测试层次与仍需真机确认的事项见 [0.6 花园说明](docs/RELEASE-0.6.0.md)；电量模型见 [0.5 模型说明](docs/RELEASE-0.5.0.md)。通过模拟器不等同于已提交审核或已在 iPhone 完成授权与键盘验证。
