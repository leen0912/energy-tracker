# 个人番茄时钟 App PRD 草稿

## 1. 背景

现在很多番茄时钟 App 开始订阅制或功能付费，用户希望拥有一个自己可控、无广告、无订阅、可长期使用的番茄时钟工具。

这个项目的目标不是做一个复杂的效率平台，而是先做一个真正顺手、轻量、稳定、可自定义、能陪伴日常专注的个人番茄时钟 App。

## 2. 产品目标

### 2.1 核心目标

- 打开即可开始专注，不需要登录或复杂配置。
- 支持开放式开始：不要求用户预估要做多久。
- 时钟和统计在后台自动归档，不把“设置计时”作为主要门槛。
- 根据睡眠、饮食、咖啡因、专注消耗等因素，帮助用户理解当天的精力状态。
- 生成漂亮、可回看的日历和总结图，让用户获得“我在一天天变好 / 我最近真的很忙”的确认感。
- 支持生成可分享的日总结、周总结或月总结卡片。
- 支持标准番茄工作流作为可选能力：专注、短休息、长休息。
- 支持必要的自定义，但默认体验应尽量少设置。
- 有明确、舒服的视觉反馈和提醒。
- 数据尽量掌握在用户自己手里。
- 技术上方便部署、维护和未来扩展。

### 2.2 非目标

- 第一版不做多人协作。
- 第一版不做社交、排行榜、打卡社区。
- 第一版不做复杂项目管理系统。
- 第一版不强依赖云端账号体系。
- 第一版不提供医学诊断、健康治疗建议或营养处方。

## 3. 目标用户

### 3.1 主要用户

- 个人自用，尤其是学习、写作、编程、阅读、工作时需要专注计时的人。
- 希望工具安静、无打扰、不被订阅绑架的人。

### 3.2 使用场景

- 电脑前工作时打开网页计时。
- 手机浏览器或 PWA 打开计时。
- 每天查看今日完成了几个番茄。
- 根据不同任务调整专注时长，例如 25 分钟、45 分钟、90 分钟。

## 4. 第一版功能范围

### 4.1 计时器

- 支持两种开始方式：
  - 预设倒计时：选择一个时长后开始，例如 25 分钟。
  - 开放式正计时：不预设时长，点击开始后一直记录，用户手动结束。
- 三种阶段类型：
  - 专注
  - 短休息
  - 长休息
- 默认配置：
  - 专注：25 分钟
  - 短休息：5 分钟
  - 长休息：15 分钟
  - 每完成 4 个专注番茄后进入长休息
- 支持开始、暂停、继续、重置。
- 支持跳过当前阶段。
- 页面标题显示剩余时间，方便切到其他标签页时查看。

### 4.2 开放式专注

这是第一版需要重点考虑的体验。

用户开始做某件事时，往往不知道自己需要多久，因此应允许用户直接开始专注，而不是先填写预计时长。

开放式专注规则：

- 用户可以只输入任务名，然后点击开始。
- 用户也可以不输入任务名，直接开始。
- 计时方式为正计时，例如 00:00、00:01、00:02。
- 用户手动点击完成后，本次专注被记录。
- 完成时可以选择是否把它计为一个番茄。
- 如果专注时间超过某个阈值，例如 20 分钟，可以默认建议计为一个番茄。
- 完成后可以进入休息建议，但不强制。

开放式专注适合：

- 不确定要做多久的任务。
- 写作、编程、阅读、整理资料等容易进入心流的任务。
- 不想被 25 分钟切断的工作。

预设倒计时适合：

- 想用标准番茄节奏训练专注。
- 明确只想做一小段时间的任务。
- 需要用提醒打断自己的场景。

### 4.3 自定义设置

- 可设置：
  - 专注时长
  - 短休息时长
  - 长休息时长
  - 几个番茄后长休息
  - 是否自动进入下一阶段
  - 是否播放提示音
  - 是否使用浏览器通知
- 可设置默认开始方式：
  - 默认开放式正计时
  - 默认预设倒计时
- 设置保存在本地。

### 4.4 提醒

- 阶段结束时播放提示音。
- 支持浏览器桌面通知。
- 如果浏览器不允许通知，仍然通过页面状态和声音提示。
- 开放式专注不会自动结束，但可以在达到建议休息点时温和提醒一次，例如“已经专注 25 分钟，要休息一下吗？”。

### 4.5 今日统计

- 展示今日完成番茄数。
- 展示今日累计专注时长。
- 可展示最近 7 天的简单趋势。
- 区分展示：
  - 标准番茄数
  - 开放式专注总时长
- 今日结束时可以生成一张总结卡片。

### 4.6 任务备注

- 当前番茄可以填写一个简单任务名，例如“写 PRD”、“读论文”、“改代码”。
- 完成后记录到本地历史。
- 第一版不做复杂任务列表，只做轻量备注。

### 4.7 PWA 能力

- 支持安装到桌面或手机主屏幕。
- 支持离线打开基础功能。
- 本地数据不依赖服务器。

### 4.8 精力电量

这个模块用于回答一个更底层的问题：

> 我今天是适合深度工作，还是应该降低目标、做轻一点的事？

它不是医学判断，而是一个个人状态参考。系统根据用户主动输入和后台记录，估算今天的“精力电量”。

可能纳入的因素：

- 睡眠：
  - 昨晚睡了多久。
  - 主观睡眠质量：差、一般、不错。
  - 是否熬夜或早醒。
- 饮食：
  - 是否吃了早餐 / 午餐 / 晚餐。
  - 是否长时间空腹。
  - 是否饮水过少。
- 咖啡因：
  - 咖啡、茶、能量饮料摄入时间。
  - 摄入量粗略等级：少量、普通、多。
  - 下午或晚上摄入可能影响晚间恢复。
- 专注消耗：
  - 今日累计专注时长。
  - 连续专注时长。
  - 中断次数。
  - 是否已经完成高强度任务。
- 主观状态：
  - 当前感觉：低电量、还可以、状态好。
  - 情绪压力：平稳、焦虑、烦躁、疲惫。

输出方式：

- 展示一个轻量的“今日电量”状态，例如：
  - 低电量
  - 可运行
  - 适合深潜
  - 需要恢复
- 给出行动建议，例如：
  - “先做 10 分钟热身任务。”
  - “今天适合收尾和整理，不适合硬开大项目。”
  - “已经连续专注较久，可以先休息。”
  - “下午咖啡因较晚，晚上可以减少刺激输入。”

输入原则：

- 不要求完整打卡。
- 允许用户只点一个状态，例如“我现在低电量”。
- 睡眠、饮食、咖啡因可以用极简按钮记录。
- 如果用户不输入，系统只根据专注记录做弱估计。
- 所有建议都应该是温和的，不使用责备或绩效化语言。

第一版可做最小版本：

- 今日主观电量选择：低 / 中 / 高。
- 睡眠简记：少 / 一般 / 够。
- 饮食简记：空腹 / 吃过。
- 咖啡因简记：无 / 上午 / 下午 / 晚上。
- 根据状态推荐开始方式：
  - 低电量：建议 5-10 分钟热身。
  - 中电量：建议开放式专注。
  - 高电量：建议深度专注。

### 4.9 日历热力图与总结图

这是产品的核心满足感来源之一。

用户不是只想完成一个计时，而是想在回头看的时候感到：

> 原来我这段时间真的有在努力。

以及：

> 原来我最近这么忙，难怪我累。

核心功能：

- 日历热力图：
  - 按天展示专注投入强度。
  - 可以按周、月查看。
  - 颜色不只表示“越多越好”，也可以表达状态，例如恢复日、低电量日、过载日。
- 日总结图：
  - 今日专注总时长。
  - 今日开始了几次。
  - 最长连续专注。
  - 当前精力状态。
  - 今日一句话总结。
- 周总结图：
  - 本周总专注时长。
  - 最忙的一天。
  - 最稳定的时间段。
  - 低电量天数。
  - 恢复是否足够。
- 月总结图：
  - 月度热力图。
  - 本月累计专注。
  - 本月高峰日。
  - 本月低电量模式。
  - 与上月相比的变化。

分享卡片原则：

- 图片要漂亮，适合保存或发给朋友。
- 默认不暴露具体任务内容，保护隐私。
- 可以选择显示 / 隐藏：
  - 专注时长
  - 任务名称
  - 精力状态
  - 日期
  - 个人昵称
- 分享文案避免鸡血和绩效压力。
- 允许偏温柔的表达，例如：
  - “今天也有好好往前走一点。”
  - “这一周，比想象中更努力。”
  - “最近很忙，也确实撑了很久。”

视觉方向：

- 日历要有“翻日历”的满足感。
- 分享图不做排行榜风格。
- 颜色应该能表达状态，而不是单纯制造焦虑。
- 低电量日也应该被温柔记录，不应该显得像失败。

第一版最小能力：

- 今日总结卡片。
- 最近 30 天日历热力图。
- 一键保存图片。
- 可选择是否隐藏任务名。

## 5. 数据与隐私

### 5.1 第一版建议

第一版优先采用本地存储，不要求注册登录。所有配置和统计默认存在浏览器本地。

优点：

- 实现简单。
- 没有服务器成本。
- 隐私最好。
- 部署和维护压力小。

缺点：

- 换设备后数据不同步。
- 清空浏览器数据会丢失记录。

### 5.2 未来可选

后续如果确实需要多设备同步，可以再加后端和账号体系。

可选同步方式：

- 登录账号后云端同步。
- 导出 / 导入 JSON 文件。
- 使用第三方云盘或数据库做轻量同步。

## 6. 体验原则

- 第一屏就是计时器，不做营销落地页。
- 视觉安静、有专注感，但不要过度装饰。
- 操作区要清楚，不需要用户读说明才会用。
- 移动端和桌面端都要可用。
- 关键按钮要大，状态要明显。
- 不用弹窗打扰用户，除非是必要权限请求。

## 7. 待讨论需求

这些需要我们一起决定：

- 是否需要账号登录？
- 是否必须多设备同步？
- 是否需要手机 App，还是 PWA 就够？
- 是否需要白噪音、背景音乐？
- 是否需要任务分类或项目维度？
- 是否需要导出 CSV / JSON？
- 是否需要日历热力图？
- 是否需要严格模式：开始后不能暂停或跳过？
- 默认开始方式应该是开放式正计时，还是标准 25 分钟倒计时？
- 开放式专注多久后建议休息：25 分钟、45 分钟，还是用户自己设置？
- 开放式专注结束后，是否自动折算番茄数？
- 是否需要网站公开访问，还是只在自己电脑 / 内网使用？
- 是否希望支持中文、英文双语？
- 是否需要深色模式？
- 是否需要“精力电量”作为核心模块？
- 精力电量是只在首页轻提示，还是作为一个独立页面？
- 睡眠、饮食、咖啡因记录要做到多细？
- 是否允许系统根据低电量状态自动推荐更轻的任务模式？
- 总结图和分享卡片应该是什么风格：温柔、极简、手帐、数据仪表盘，还是日历照片感？
- 分享卡片默认展示哪些信息，哪些必须隐藏？
- 热力图应该只表示专注时长，还是同时表达精力状态和过载程度？

## 8. 技术路径讨论

### 8.1 路径 A：纯前端静态 PWA

前端：

- HTML / CSS / JavaScript，或 Vite + React / Vue / Svelte。
- 数据存在 localStorage 或 IndexedDB。
- PWA Service Worker 支持离线。

后端：

- 无后端。

部署：

- GitHub Pages
- Vercel
- Netlify
- Cloudflare Pages
- 本地静态服务器

优点：

- 最简单、最便宜。
- 几乎零维护。
- 很适合个人工具第一版。
- 可以一键部署成网页，也可以安装成 PWA。

缺点：

- 无法天然多设备同步。
- 浏览器本地数据需要备份能力兜底。

推荐度：高，适合作为第一版。

### 8.2 路径 B：前端 PWA + 轻量后端

前端：

- React / Vue / Svelte 任一框架。
- PWA 负责主要体验。

后端：

- Node.js + SQLite / PostgreSQL
- 或 Supabase / Firebase / PocketBase

部署：

- 前端：Vercel / Netlify / Cloudflare Pages
- 后端：Render / Railway / Fly.io / 自己的 VPS
- 数据库：Supabase / Neon / Railway PostgreSQL / SQLite on VPS

优点：

- 可做账号登录和多设备同步。
- 未来可以扩展统计、任务、备份。

缺点：

- 成本和维护明显增加。
- 账号、权限、数据安全要认真处理。
- 对第一版来说可能偏重。

推荐度：中，适合确认需要同步后再做。

### 8.3 路径 C：本地桌面 App

技术：

- Tauri + 前端框架
- 或 Electron

部署：

- 直接安装在自己的电脑上。

优点：

- 更像原生 App。
- 可以做系统托盘、全局快捷键、本地文件存储。
- 不依赖浏览器标签页。

缺点：

- 打包和更新更麻烦。
- 手机端不适用。
- 第一版成本比网页 PWA 高。

推荐度：中低，除非非常想要桌面原生体验。

### 8.4 路径 D：移动端原生 App

技术：

- React Native
- Flutter
- Swift / Kotlin

部署：

- iOS / Android 本地安装或应用商店。

优点：

- 手机体验最好。
- 通知和后台计时能力更强。

缺点：

- 开发和发布成本最高。
- 上架、证书、系统权限都更麻烦。

推荐度：低，不建议第一版直接做。

### 8.5 路径 E：微信小程序

技术：

- 微信原生小程序：WXML + WXSS + JavaScript / TypeScript。
- 或使用跨端框架：Taro / uni-app，将来可兼顾 H5 和小程序。

部署：

- 微信公众平台小程序后台提交审核并发布。
- 开发和预览使用微信开发者工具。

后端选择：

- 无后端：只用小程序本地存储，适合个人计时、设置和本机统计。
- 微信云开发 CloudBase：用于多设备同步、云端统计、备份。
- 自建后端：用于完全自控，但需要服务器、域名、备案和运维。

优点：

- 打开方便，微信内就能用。
- 手机端体验自然，不需要引导安装 PWA。
- 如果使用微信云开发，登录身份和云端数据会比较顺。

缺点：

- 需要微信小程序账号、审核、发布流程。
- 正式上线通常涉及认证 / 年审、备案等平台要求。
- 和 Web / PWA 不是同一个运行环境，不能完全原样部署。
- `web-view` 套网页的方式受主体类型、HTTPS 域名、业务域名和备案限制，不建议作为主方案。

成本判断：

- 如果只做自己测试和开发预览：基本可以零成本。
- 如果正式发布为个人工具小程序：可能产生小程序认证 / 年审成本，个人主体常见为较低年费，企业主体更高，最终以微信公众平台后台为准。
- 如果不用云端同步：后端成本为 0。
- 如果使用微信云开发：前期可评估免费体验环境；正式长期使用要按 CloudBase 当前套餐或资源点计费。
- 如果自建后端：还会有服务器、域名、HTTPS、备案和运维成本。

推荐度：中高，前提是主要使用场景在手机微信内。如果目标是电脑和手机都舒服使用，可以考虑先做 PWA，再用 Taro / uni-app 或重写小程序版。

## 9. 初步技术建议

第一版建议采用：

- 前端：Vite + React + TypeScript
- 样式：CSS Modules 或普通 CSS
- 存储：IndexedDB 或 localStorage
- 部署：Cloudflare Pages / Vercel / GitHub Pages 三选一
- 后端：暂不需要

如果优先选择微信生态，则第一版建议改为：

- 小程序端：Taro / uni-app 或微信原生小程序
- 存储：本地存储优先
- 后端：暂不需要
- 云端同步：作为第二阶段再加微信云开发 CloudBase

理由：

- React + TypeScript 维护性较好。
- PWA 能满足“自己用”“手机桌面快捷方式”“离线可用”的需求。
- 没有后端可以避免服务器费用和账号系统复杂度。
- 以后如果要同步，可以再补后端，不影响第一版价值。

## 10. 第一版验收标准

- 用户打开页面后 3 秒内能开始一个专注计时。
- 用户不填写预计时长也能开始专注。
- 用户可以用一次点击记录当前精力状态。
- 系统可以根据低 / 中 / 高电量推荐不同的开始方式。
- 用户可以查看最近 30 天日历热力图。
- 用户可以生成并保存今日总结图。
- 专注、短休息、长休息流程正确。
- 开放式正计时可以开始、暂停、继续、完成，并记录实际时长。
- 自定义设置刷新页面后仍然保留。
- 今日统计刷新页面后仍然保留。
- 阶段结束有声音或通知提醒。
- 桌面端和手机端布局都不拥挤。
- 可以部署为一个可访问的网址。
- 可以安装为 PWA。

## 11. 开放问题

1. 你更希望它是“极简计时器”，还是“带一点任务记录的专注日志”？
2. 数据是否必须跨设备同步？
3. 你主要会在电脑用，还是手机也会频繁用？
4. 你是否接受第一版无账号、数据只存在本地？
5. 部署地点倾向哪里：GitHub Pages、Vercel、Cloudflare Pages，还是只在本地电脑使用？
6. 视觉风格想要哪类：清爽工具型、温柔治愈型、黑白极简型、还是更像专业工作台？
7. 开始专注时，你更想先输入任务名，还是直接点开始？
8. 开放式专注结束后，系统要不要帮你自动判断这算几个番茄？
9. 你希望它更像“专注工具”，还是更像“低电量生活管理器”？
10. 精力电量记录里，哪些信息你愿意每天顺手点一下：睡眠、吃饭、咖啡因、情绪、压力？
11. 你希望总结图更偏“可爱手帐”，还是“高级数据图”，还是“安静日历”？
12. 分享图是给自己保存为主，还是发朋友圈 / 发朋友为主？
